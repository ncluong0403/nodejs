// import { Request } from 'express'

// export const checkIfEmailExists = (value: string, req: Request) => {
//   if (value !== req.body.password) {
//     throw new Error('Password confirmation does not match password')
//   }
//   return true
// }
